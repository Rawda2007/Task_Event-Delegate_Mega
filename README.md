# Task_Event-Delegate_Mega
### Create OrderService class with an Event.
### Create 2 subscriber classes (EmailService, SMSService).
### Notify subscribers when order is placed.
### Use Lambda Expression to Send Watsapp Message.
### Add one Extension Method (e.g., format order (OrderID , Order Name ).
### Use Predicate to filter Amount orders and ID Order greater  than 0 before notifying.
